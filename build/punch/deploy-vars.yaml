application:
  tenant: kast
  file_size: 500MB
metadata:
  elasticsearch:
    hosts:
      - http://punchplatform-es-http.doc-store:9200
    credentials:
      username: elastic
      password: elastic
    ssl_enabled: false
    ssl_insecure: false
    ssl_hostname_verification: false

data:
  minio:
    host: http://minio.object-store:9002
    access_key: minio
    secret_key: K@st2020*
